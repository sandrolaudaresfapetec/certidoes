-- Geometrias persistidas como GeoJSON (TEXT). Cortes executados em Turf.js (Node).
-- A extensao PostGIS (colunas geometry nativas) fica para uma fase futura,
-- quando o cluster for migrado para a imagem postgis/postgis:16-3.4.
SELECT 1;
